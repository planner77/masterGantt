name: Fix task recovery notification once

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/fix-task-recovery-notification-once.yml

permissions:
  contents: write

jobs:
  fix_and_verify:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: true
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: 22
          cache: npm
      - name: Patch recovery notification order
        run: node scripts/fix-recovery-notification-order.mjs
      - run: npm ci
      - run: npm run typecheck
      - run: npm run lint
      - run: npx --no-install playwright install --with-deps chromium
      - name: Verify failing E2E
        run: npx --no-install playwright test tests/e2e/project-task-persistence.spec.ts --config tests/config/playwright.config.ts
      - name: Commit fix and cleanup
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm scripts/fix-recovery-notification-order.mjs
          git rm .github/workflows/fix-task-recovery-notification-once.yml
          if [ -f .github/workflows/one-time-release-v0.13.0-retry.yml ]; then git rm .github/workflows/one-time-release-v0.13.0-retry.yml; fi
          git add src/features/projects/project-readonly-view.tsx
          git diff --cached --check
          git commit -m "fix: 일정 복구 실패 알림을 안정적으로 보존"
          git push origin HEAD:main
