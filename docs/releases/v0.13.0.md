# masterGantt v0.13.0

Issue #28의 Excel Grid/Gantt 내보내기 기능을 추가하는 하위 호환 MINOR 버전이다.

## 주요 변경

- 프로젝트 화면에서 Excel(`.xlsx`) 내보내기 진입점 제공
- Grid와 Gantt timeline을 동일 작업 행으로 표현하고 WBS/계층, 시작·종료일, 근무일 기간, 진행률 보존
- 매 실행 시 작업 관계 `포함 / 제외 / 취소`를 명시적으로 선택
- 관계 포함 시 `Dependencies` 시트와 Gantt DrawingML 연결 화살표 생성
- 관계 제외 시 관계 시트·열·Drawing relationship을 생성하지 않음
- canonical Project snapshot과 strong `If-Match`를 사용하여 사용자가 확인한 revision의 읽기 전용 데이터만 출력
- SVAR PRO export 및 외부 Excel SaaS/ExcelJS를 사용하지 않고 Node 표준 기능 기반 내부 OOXML/XLSX writer 사용
- 사용자 문자열은 수식이 아닌 문자열 셀로 기록하고 매크로·외부 데이터 연결을 생성하지 않음
- Excel 내보내기 버튼을 기존 프로젝트 헤더 action 영역에 통합하여 Gantt workspace의 기존 좌표·높이 계약을 유지

## 버전 정책

사용자에게 노출되는 신규 기능이므로 Semantic Versioning의 MINOR 변경으로 분류하여 `0.12.0`에서 `0.13.0`으로 증가한다. `package.json`과 `package-lock.json`은 동일한 `0.13.0`으로 유지한다.

## 구현 문서

현재 구현 계약, workbook 구조, 보안 정책 및 한도는 `docs/EXCEL_EXPORT.md`를 따른다.

`docs/IMPORT_EXPORT.md`의 Excel Export Phase 1 부분은 초기 계획 문서이며 실제 구현과 충돌하는 경우 `docs/EXCEL_EXPORT.md`가 우선한다.

## 검증

PR #55 head `6d30120bbd8b075d7a1b90ed485fddffa3ebc5b7` 기준 GitHub Actions CI Run #252에서 다음 gate가 모두 PASS했다.

- release version consistency
- TypeScript / ESLint
- unit test / dependency audit
- Markdown link / deployment shell validation
- production build
- Chromium E2E 전체 suite
- Docker image policy / migration / readiness / SQLite restart persistence
- production HTTP/HTTPS cookie, auth, Origin/revision transport regression
- relocated Compose persistence

PR #55 병합 commit `0d9e07b6883fbf405741b82642ca7c167e5a9b28`에 대한 main CI Run #255에서도 다음 필수 gate가 모두 PASS했다.

- Build, static checks, and unit tests
- Chromium end-to-end tests
- Docker build and runtime smoke test

main CI의 `Publish and verify immutable main commit image` job과 후속 v0.13.0 게시 자동화는 GitHub Actions runner가 배정되기 전에 `steps=[]`, `runner_id=0` 상태로 반복 실패했다. 따라서 이 문서 갱신 시점 기준으로 `v0.13.0` GHCR release image, annotated tag, GitHub Release는 아직 게시되지 않았다. 애플리케이션 검증 실패와 구분하며, Actions 실행 인프라가 복구되면 기존 `release-image.yml` 절차를 재실행한다.

Windows Excel 2021 및 조직 DRM 환경에서의 실제 열기·표시 검증은 GitHub-hosted runner가 대체하지 않으며 별도 Environment-specific Validation 대상으로 유지한다.
